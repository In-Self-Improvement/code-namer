name: Review Assign

on: 
  pull_requeset:
    types: [opened, ready_for_review]

jobs:
  assign:
    runs-on: ubuntu-lastest
    steps:
      - users: gkusu/review-assgin-action@v1
        with:
          assignees: ${{ github.actor}}
          
